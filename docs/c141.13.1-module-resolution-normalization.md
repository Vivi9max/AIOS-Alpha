# C141.13.1 — Module Resolution Normalization

Status: Specification correction for C141.13; Production‑blocking defect identified in real repository
Repository: Vivi9max/AIOS‑Alpha
Branch: main

## 1. Defect Summary
Real‑world repository observation:
1. `lib/github/task‑dispatch.ts` correctly invokes `assertC14113AutonomousSafetyBeforeWrite()` **before `writeGitHubFile()`**; pre‑write interception position is correct ✅.
2. The runtime adapter file exists on disk, but uses **Unicode non‑breaking hyphen** instead of standard ASCII hyphen `-`.
3. Normal ASCII‑path import resolution returns 404 under build / runtime.
4. Adapter internal import `./c141.13‑safety‑gate` also carries the same Unicode character risk.
5. `c141.13‑safety‑gate.ts` existence and module resolution status is unconfirmed.
6. Production build, live founder test, deny‑path and allow‑path safety gate tests are **not yet executed**.

> Current conclusion: C141.13 cannot be marked VERIFIED for production.
> Patch file presence alone is not sufficient proof of valid integration.

## 2. Mandatory Normalization Requirement
All C141.13 module filenames and import paths **MUST use plain ASCII hyphen `-`**.
No visually‑similar Unicode punctuation (non‑breaking hyphen, en‑dash, em‑dash) shall appear in filenames or import statements.

Canonical correct file names:
- `lib/github/c141.13‑safety‑runtime.ts`
- `lib/github/c141.13‑safety‑gate.ts`

Valid module resolution chain, ASCII‑only:
task‑dispatch.ts
↓
c141.13‑safety‑runtime.ts
↓
c141.13‑safety‑gate.ts
## 3. Required Fix Actions
1. Rename the existing adapter file, replacing Unicode hyphen with standard ASCII `-`.
2. Update all import statements inside `c141.13‑safety‑runtime.ts` to use ASCII‑only filename for `c141.13‑safety‑gate.ts`.
3. Confirm `c141.13‑safety‑gate.ts` file physically exists with ASCII filename.
4. Verify the full import chain resolves without 404 / module‑not‑found errors in **Production build environment**.

## 4. Post‑Fix Validation Sequence (Must run in exact order)
Build
↓
Deploy
↓
Founder Live Test
↓
Safety Gate DENY test
↓
Safety Gate ALLOW test
↓
WRITE
↓
READBACK
↓
VERIFIED
### Individual checkpoints
- [ ] Production build completes without module resolution errors
- [ ] Founder live test environment available
- [ ] Safety‑Gate‑DENY test: write is blocked, returns `AUTONOMOUS_SAFETY_GATE_DENIED`, no GitHub commit created
- [ ] Safety‑Gate‑ALLOW test: write proceeds, commit created
- [ ] Readback succeeds, content matches proposed content
- [ ] Runtime returns `GITHUB_WRITE_VERIFIED` / `readbackVerified: true`

Only after all above pass, C141.13 can be marked VERIFIED.

## 5. Acceptance Statement
C141.13.1 fixes the Unicode filename module‑resolution defect blocking production validation.
This specification defines the correction contract and required end‑to‑end test sequence.

## 6. Commit
`docs(C141.13.1): module resolution normalization, fix unicode filename defect`
